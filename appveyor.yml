environment:
  matrix:
    - node_version: "8"

branches:
  only:
  - appveyor
  
install:
  - ps: Install-Product node $env:node_version
  - yarn install

test_script:
  - node --version
  - yarn run coveralls
  
cache:
 - node_modules
 - "%LOCALAPPDATA%/Yarn"
